name: CrashLens
description: "Computer-vision adjuster that segments damage, classifies parts, and returns a part-level loss report with repair/replace, hours, and confidence."
emoji: "🔬"

# Document processing configuration
document_access:
  enabled: true
  supported_formats: ["txt", "csv", "pdf", "docx", "pptx", "jpg", "jpeg", "png", "gif"]
  max_file_size_mb: 5
  ai_extraction:
    enabled: true
    generate_summary: true
