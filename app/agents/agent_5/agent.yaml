name: PolicyLink
description: "Policy-status verifier that resolves the policy for a vehicle, validates active coverage on the loss date, and returns deductibles, IDV, NCB, add-ons, and exclusions."
emoji: "🎯"

# Document processing configuration
document_access:
  enabled: true
  supported_formats: ["txt", "csv", "pdf", "docx", "pptx", "jpg", "jpeg", "png", "gif"]
  max_file_size_mb: 5
  ai_extraction:
    enabled: true
    generate_summary: true
